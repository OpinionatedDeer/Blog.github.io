I had to change this

- name: Install Hugo
  run: |
    echo "Installing Hugo ${HUGO_VERSION}..."
    curl -sfL --output-dir "${{ runner.temp }}" -O "https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
    mkdir "${HOME}/.local/hugo"
    tar -C "${HOME}/.local/hugo" -xf "${{ runner.temp }}/hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz"
    echo "${HOME}/.local/hugo" >> "${GITHUB_PATH}"


difference
- hugo_${HUGO_VERSION}_linux-amd64.tar.gz
+ hugo_extended_${HUGO_VERSION}_linux-amd64.tar.gz


also i am currently in 147.7 version instead of official 168 something

I will need to change languagecode to locale
and wait till the theme gets update to new api before i can continue updating go

It was supposed to be simple :v
oh well
